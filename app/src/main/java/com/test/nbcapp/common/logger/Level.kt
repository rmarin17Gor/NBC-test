package com.test.nbcapp.common.logger

/**
 * Enum class that handle the different types of level to be logged.
 */
enum class Level {
    VERBOSE,
    DEBUG,
    INFO,
    WARN,
    ERROR,
    ASSERT
}
