package com.test.nbcapp.home.domain.model

/**
 * Enum class that handle the different types of shelves.
 */
enum class ShelfType(val value: String) {
    LIVE("Live"), EPISODE("Episode"), SHOW("Show")
}
