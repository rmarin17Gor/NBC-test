package com.test.nbcapp.home.domain.model

/**
 * Enum class that handle the different types of shelf section.
 */
enum class ShelfSection(val value: String) {
    CONTINUE_WATCHING("Continue Watching"),
    TRENDING_NOW("Trending Now"),
    LATEST_EPISODE("Latest Episodes")
}
